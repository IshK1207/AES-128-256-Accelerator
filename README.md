# AES-128-256-Accelerator
This repo contains the RTL code of an AES encryption accelerator which can be configured for both AES-128 and AES-256 in ECB mode.  This also contains the driver code for the accelerator which can be connected through an AXI-Lite interface.
